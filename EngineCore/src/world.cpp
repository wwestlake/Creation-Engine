#include "engine/world.h"

// World is currently header-only logic over entt::registry; this
// translation unit exists so the target has a stable .cpp to grow into as
// entity lifecycle/query helpers are added.
